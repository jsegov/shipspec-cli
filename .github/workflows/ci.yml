name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: Format, Lint, Type Check, Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v4

      - name: Set up Node.js
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f # v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run format check
        run: npm run format:check

      - name: Run type check
        run: npm run typecheck

      - name: Run linter
        run: npm run lint

      - name: Build project
        run: npm run build

      - name: Run tests
        run: npm test

      - name: Verify package contents
        run: |
          npm pack --dry-run > pack-output.txt 2>&1
          echo "=== Package Contents ==="
          cat pack-output.txt
          
          # Fail if any .map files are included
          if grep -q '\.map$' pack-output.txt; then
            echo "ERROR: Source map files found in package!"
            grep '\.map$' pack-output.txt
            exit 1
          fi
          
          # Verify essential files are present
          if ! grep -q 'dist/cli/index.js' pack-output.txt; then
            echo "ERROR: CLI entry point missing from package!"
            exit 1
          fi
          
          echo "✓ Package contents verified"

